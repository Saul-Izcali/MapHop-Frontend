import { BusquedaUsuariosPipe } from './busqueda-usuarios.pipe';

describe('BusquedaUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
